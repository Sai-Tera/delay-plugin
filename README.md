# delay-plugin
This delay plugin was developed as part of the latency mismatch experiment. It accepts an input delay value (system latency) in milliseconds, subtracts the selected latency offset, and applies the resulting delay to the signal.
